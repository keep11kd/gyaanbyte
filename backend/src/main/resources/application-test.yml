server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gyaanbyte_test
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
